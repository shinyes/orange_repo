// 管理端 API（占位）：第二阶段把 web/（管理端）迁入并合并管理接口至此处。
// 现仅保留命名空间与说明，避免门户逻辑引用；接入后按资源分模块组织（如 users/domains/...）。
// 接口前缀沿用主站管理路由（合并后仍为 /api/...，由 src/api/client.ts 统一 fetch/401 处理）。
export const adminApi = {
  // TODO(admin): 第二阶段迁入 web/ 管理接口
}
